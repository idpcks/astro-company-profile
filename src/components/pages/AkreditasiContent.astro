---
/**
 * Isi halaman /akreditasi — Akreditasi (submenu "Tentang Kami").
 */
import CTASection from '@/components/sections/CTASection.astro';
import PageHero from '@/components/ui/PageHero.astro';
import SectionHeading from '@/components/ui/SectionHeading.astro';
import { tFactory, type Lang } from '@/i18n';

interface Props {
  lang: Lang;
}

const { lang } = Astro.props;
const t = tFactory(lang);
---

<PageHero
  eyebrow={t('pagehero.eyebrow.akreditasi')}
  title={t('akreditasiPage.hero.title')}
  lead={t('akreditasiPage.hero.lead')}
/>

<section class="section">
  <div class="container">
    <SectionHeading title={t('akreditasiPage.body.heading')} align="start" />
    <div class="akreditasi__body">
      <p>{t('akreditasiPage.body.para1')}</p>
      <p>{t('akreditasiPage.body.para2')}</p>
    </div>
  </div>
</section>

<CTASection lang={lang} />

<style>
  .akreditasi__body {
    display: grid;
    gap: var(--space-4);
    max-width: 42rem;
  }
</style>
