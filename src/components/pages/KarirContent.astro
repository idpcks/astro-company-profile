---
/**
 * Isi halaman /karir — dipakai bersama oleh root (id) & [lang] (en).
 */
import CTASection from '@/components/sections/CTASection.astro';
import EmptyState from '@/components/ui/EmptyState.astro';
import PageHero from '@/components/ui/PageHero.astro';
import { localePath, tFactory, type Lang } from '@/i18n';

interface Props {
  lang: Lang;
}

const { lang } = Astro.props;
const t = tFactory(lang);
---

<PageHero
  eyebrow={t('pagehero.eyebrow.karir')}
  title={t('page.title.karir')}
  lead={t('page.description.karir')}
/>

<section class="section">
  <div class="container">
    <EmptyState
      icon="briefcase"
      title={t('karir.empty.title')}
      description={t('karir.empty.description')}
      actionText={t('karir.empty.action')}
      actionHref={localePath('/contact', lang)}
    />
  </div>
</section>

<CTASection lang={lang} />
