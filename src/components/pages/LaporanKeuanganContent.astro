---
/**
 * Isi halaman /laporan-keuangan — dipakai bersama oleh root (id) & [lang] (en).
 */
import CTASection from '@/components/sections/CTASection.astro';
import EmptyState from '@/components/ui/EmptyState.astro';
import PageHero from '@/components/ui/PageHero.astro';
import { localePath, tFactory, type Lang } from '@/i18n';

interface Props {
  lang: Lang;
}

const { lang } = Astro.props;
const t = tFactory(lang);
---

<PageHero
  eyebrow={t('pagehero.eyebrow.laporanKeuangan')}
  title={t('page.title.laporanKeuangan')}
  lead={t('page.description.laporanKeuangan')}
/>

<section class="section">
  <div class="container">
    <EmptyState
      icon="file-text"
      title={t('laporanKeuangan.empty.title')}
      description={t('laporanKeuangan.empty.description')}
      actionText={t('laporanKeuangan.empty.action')}
      actionHref={localePath('/contact', lang)}
    />
  </div>
</section>

<CTASection lang={lang} />
