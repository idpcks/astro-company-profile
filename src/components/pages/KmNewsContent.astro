---
/**
 * Isi halaman /km-news — dipakai bersama oleh root (id) & [lang] (en).
 */
import CTASection from '@/components/sections/CTASection.astro';
import EmptyState from '@/components/ui/EmptyState.astro';
import PageHero from '@/components/ui/PageHero.astro';
import { localePath, tFactory, type Lang } from '@/i18n';

interface Props {
  lang: Lang;
}

const { lang } = Astro.props;
const t = tFactory(lang);
---

<PageHero
  eyebrow={t('pagehero.eyebrow.kmNews')}
  title={t('page.title.kmNews')}
  lead={t('page.description.kmNews')}
/>

<section class="section">
  <div class="container">
    <EmptyState
      icon="newspaper"
      title={t('kmNews.empty.title')}
      description={t('kmNews.empty.description')}
      actionText={t('kmNews.empty.action')}
      actionHref={localePath('/blog', lang)}
    />
  </div>
</section>

<CTASection lang={lang} />
